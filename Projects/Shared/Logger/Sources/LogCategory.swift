public enum LogCategory: String, Sendable {
    case ui = "UI"
    case network = "Network"
    case storage = "Storage"
    case auth = "Auth"
    case cache = "Cache"
    case general = "General"
}
